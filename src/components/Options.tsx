import React from 'react';
import '../styles.scss';

const Options = () => {
    return (
        <div className="info-list">
            <div>
                <p>Total results: </p>
            </div>
        </div>
    );
};

export default Options;
