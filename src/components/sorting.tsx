import React from 'react';

const Sorting = ({ pics }: any) => {
    return (
        <div>
            <a href="#">Popularity</a>|<a href="#">Size</a>|<a href="#">Date</a>
        </div>
    );
};

export default Sorting;
